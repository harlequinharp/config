#!/bin/bash
eval $("gpg-agent --daemon")

setxkbmap \
    -layout "us, dvp" \
    -option "grp:shift_caps_toggle"

xsetroot \
    -cursor_name left_ptr
/home/allie/config/scripts/synergy_ssh.sh
unclutter &
redshift -l 42.3:-71.059 &
dbus-launch --exit-with-session xmonad
